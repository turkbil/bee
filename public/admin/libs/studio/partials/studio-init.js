/**
 * Studio Editor - Başlatıcı
 * Studio Editor'ün sayfa yüklendiğinde başlatılmasını yönetir
 */

// Bu dosyadaki DOMContentLoaded olayını tamamen devre dışı bırak çünkü app.js bunu zaten yapıyor
// Eğer bu dosyayı tamamen silemiyorsak, tüm işlevleri devre dışı bırakalım

(function() {
    // Bu dosyada hiçbir olay dinleyicisi eklenmesin, app.js'e bunu bırakalım
    console.log('studio-init.js artık kullanılmıyor, app.js kullanılıyor');
})();